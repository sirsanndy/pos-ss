version: '3.8'
services:
  postgres:
    image: postgres:latest
    container_name: pos-db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: pos-api
    ports:
      - "8778:5432"
    volumes:
      - /Users/muhamad.hasanudin/projects/container/pos-be:/var/lib/postgresql/data